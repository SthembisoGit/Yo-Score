# Scripts Folder

This folder is reserved for repository-level helper scripts used during setup, deployment, or
maintenance.

Use this location for operational helpers that do not belong strictly to `backend/` or
`ml-service/` runtime flows.
